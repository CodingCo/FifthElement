#!/usr/bin/env node
var debug = require('debug')('FifthElement/server');
var app = require('../server/app');

app.set('port', process.env.PORT || 3000);

var server = app.listen(app.get('port'), function () {
    debug('Server started on port: ' + server.address().port + " ip: " + server.address());
});
